overwrite: true
schema: "./schema/schema.graphql"
documents: null
generates:
  schema/graphql.generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
  ./graphql.schema.json:
    plugins:
      - "introspection"
